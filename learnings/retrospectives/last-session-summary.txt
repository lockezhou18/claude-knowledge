Session 2026-04-11: AgentBus fixes (command extraction + NATS auth retry), multi-turn conversation test (3-step, context retained). markitdown skill created + 3 design docs converted to memory. cmux evaluated via /delegate → skip (existing infra covers it). Push notifications: scout hook shows dream results on first prompt (replaces manual checking). VM dream cron live at 3:47am. 2 new insights: push-vs-pull notifications, markitdown adoption.
