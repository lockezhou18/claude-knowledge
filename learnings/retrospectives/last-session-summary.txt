Session 2026-04-10/11: Three major builds. (1) Self-improving eval system: Oracle pattern → eval pipeline → judged 35 sessions (76.2% agreement, 85.1% principle adherence) → 4 fix iterations on behavioral gates + principle adherence dimension. (2) AgentBus VM agent: cloned agentbus repo → implemented Claude adapter → deployed NATS + listener on VM → e2e tested (shell + thinking + async + multi-turn sessions) → Monitor for real-time async → L1/L2 triage. (3) /delegate skill: natural language, no flags needed. Fixed: --bare auth bug, port 14222, session resume, envelope parsing. Key: Monitor tool + NATS subscriber is the right async pattern.
