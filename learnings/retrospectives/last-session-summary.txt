Session 2026-04-08: VM Bootstrap + Remote Build Infrastructure

Bootstrapped bizhou-ld2.linkedin.biz (Claude CLI, code-server, tmux, knowledge).
Passwordless SSH via config.custom + LinkedIn key. Created vm-run helper for
generic sync-and-build from any local repo. PreToolUse hook auto-intercepts
build commands. 3-layer fallback (VM→sync→local). Built hp-ats-integration-mt
on VM successfully. SSH permission bug: Bash(ssh:*) doesn't work in Claude Code,
use bash -c wrapper. Next: verify hook in fresh session.
