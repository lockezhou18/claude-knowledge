---
name: vm-setup
description: Bootstrap a new LinkedIn dev VM
---

Run the bootstrap script: `bash -c "cd ~/claude-knowledge && ssh vm 'bash -s' < scripts/vm-bootstrap.sh"`

For manual steps or troubleshooting, see `scripts/vm-bootstrap.sh`.
